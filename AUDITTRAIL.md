# PlanaCasa — Audit Trail

Full record of every decision, file, database change, and commit made during development.
Updated after every phase. Timestamps are session-relative (date: 2026-05-29).

---

## Session 1 — Planning & Initialisation

### Decision log

| # | Decision | Rationale |
|---|----------|-----------|
| 1 | Next.js 16.2.6 (App Router, TypeScript strict) | `create-next-app` resolved to latest — spec said 14 but 16 is backward-compatible for our use |
| 2 | Tailwind v4 (not v3) | Installed automatically; brand tokens moved to `globals.css @theme` block instead of `tailwind.config.ts` |
| 3 | `middleware.ts` renamed to `proxy.ts` | Next.js 16 deprecated `middleware` file convention in favour of `proxy` |
| 4 | Leaflet (not Google Maps) for contractor map | No API key required; free tiles; swap later if needed |
| 5 | URL search params for gallery filters | Shareable links; no Zustand needed |
| 6 | `users.role` column (`customer` / `admin`) | Simpler than a separate `admin_users` table |
| 7 | `MockPaymentProvider` interface | Lets full checkout → download flow work without PayMongo/Stripe keys |
| 8 | GSAP only for hero parallax + stats counter | Heavy library — isolated to where it earns its weight |
| 9 | Framer Motion for all component-level animation | Lighter, React-idiomatic |
| 10 | Sonner for toasts | Preferred over react-hot-toast; shadcn-compatible |
| 11 | npm (not pnpm/yarn) | Default from create-next-app |
| 12 | `supabase/migrations/` tracked in git | SQL-as-code; Supabase MCP used to push (no CLI personal token needed) |

---

## Commit 1 — `af10b8e` — Initial commit from Create Next App

**Date:** 2026-05-29  
**Branch:** main  
**Pushed to GitHub:** https://github.com/devaidusty/PlanaCasa

Auto-generated by `create-next-app`. Contains only the default scaffold.

**Files:** Standard Next.js scaffold (app/, public/, package.json, tsconfig.json, tailwind.config.ts, postcss.config.mjs, eslint.config.mjs, README.md, next-env.d.ts, next.config.ts).

---

## Commit 2 — `ca3db89` — Phase 0+1: project scaffold, design system, Supabase schema, chrome

**Date:** 2026-05-29  
**Branch:** main  
**Files changed:** 46 files · +17,306 lines / −3,646 lines

### Environment & config

| File | What changed |
|------|-------------|
| `.env.local` | Created (git-ignored). Contains live Supabase URL + anon key + service role key. Placeholder values for Stripe, PayMongo, Resend. |
| `.env.local.example` | Created (committed). Safe template — no real values. |
| `.gitignore` | Updated: `.env`, `.env.local`, `.env.*.local`, `.env.production` all ignored. `.env.local.example` stays committed. |
| `package.json` | Renamed from `planacasa-temp` → `planacasa`. Added all dependencies (see below). |
| `next.config.ts` | Added `turbopack.root`, `images.remotePatterns` for Unsplash + Supabase Storage. |
| `components.json` | shadcn/ui configuration file. |

### Dependencies installed

| Package | Version | Purpose |
|---------|---------|---------|
| `@supabase/supabase-js` | ^2.106.2 | Supabase client |
| `@supabase/ssr` | ^0.10.3 | SSR-compatible Supabase auth |
| `framer-motion` | ^12.40.0 | Component animations |
| `gsap` | ^3.15.0 | Hero parallax + counter |
| `lucide-react` | ^1.17.0 | Icons |
| `react-hook-form` | ^7.76.1 | Form state management |
| `@hookform/resolvers` | ^5.4.0 | Zod adapter for RHF |
| `zod` | ^4.4.3 | Schema validation |
| `sonner` | ^2.0.7 | Toast notifications |
| `leaflet` + `react-leaflet` | ^1.9.4 / ^5.0.0 | Map (contractor marketplace) |
| `@types/leaflet` | ^1.9.21 | Types for Leaflet |
| `react-markdown` | ^10.1.0 | Guide content rendering |
| `next-pwa` | ^5.6.0 | PWA manifest + service worker |
| `tailwindcss-animate` | ^1.0.7 | Tailwind animation utilities |
| `class-variance-authority` | ^0.7.1 | shadcn variant helper |
| `clsx` + `tailwind-merge` | ^2.1.1 / ^3.6.0 | Conditional classnames |
| shadcn components | — | badge, card, input, label, select, sheet, skeleton, tabs, dialog, dropdown-menu, separator, avatar, button |

### Design system

| File | What it does |
|------|-------------|
| `app/globals.css` | Tailwind v4 `@theme inline` block — brand CSS variables: `--color-navy (#1B2A4A)`, `--color-gold (#C9A84C)`, `--color-warm-white (#F8F5F0)`, `--color-text-dark (#1A1A2E)`, `--color-text-light (#6B7280)`, `--color-success (#10B981)`, `--color-background (#FAFAF8)`. Font families wired to Tailwind utility classes. Custom scrollbar. Selection highlight. |
| `app/layout.tsx` | Loads Playfair Display (400, 700), Inter (400, 500, 600), Cormorant Garamond (400, 600) via `next/font/google`. Full SEO metadata template. Wraps all children in `AuthProvider`. Includes `Toaster` (sonner). Body has `pb-16 md:pb-0` for mobile bottom nav. |

### TypeScript types

**File:** `types/index.ts` — 233 lines  
All interfaces for the entire application:

| Type | Fields |
|------|--------|
| `DesignStyle` | Union of 9 enum values |
| `Design` | 22 fields including arrays (preview_images, floor_plan_images, tags) |
| `DesignPackage` | 6 fields |
| `Purchase` | 12 fields including payment_status, download_count |
| `User` | 11 fields including `role: 'customer' \| 'admin'` |
| `ListingTier` | Union: free / verified / featured |
| `Contractor` | 26 fields |
| `ContractorReview` | 10 fields |
| `GuideCategory` | Union of 9 enum values |
| `Guide` | 11 fields |
| `CostCalculatorData` | 7 fields |
| `CustomizationStatus` | Union: pending / reviewing / quoted / completed |
| `CustomizationRequest` | 8 fields |
| `NewsletterSubscriber` | 6 fields |
| `Wishlist` | 4 fields |
| `GalleryFilters` | 12 filter fields for gallery page |
| `CostEstimate` | Computed output from calculator |
| `CostBreakdownItem` | Per-phase cost row |
| `CheckoutData` | Checkout form shape |

### Supabase library

| File | Purpose |
|------|---------|
| `lib/supabase/client.ts` | Browser client via `createBrowserClient` from `@supabase/ssr` |
| `lib/supabase/server.ts` | Server component client via `createServerClient` + Next.js `cookies()` |
| `lib/supabase/admin.ts` | Service-role client — bypasses RLS; used only in route handlers |
| `proxy.ts` | Next.js 16 proxy (replaces middleware). Refreshes session on every request. Guards: `/dashboard/*` + `/checkout` → `/auth/login?returnTo=...` if unauthenticated. `/admin/*` → `/` if not admin role. Redirects logged-in users away from `/auth/login` + `/auth/register`. |

### Utility functions

| File | Exports |
|------|---------|
| `lib/utils/formatCurrency.ts` | `formatPHP(n)` → ₱1,234,567 · `formatPHPCompact(n)` → ₱1.2M / ₱850K · `formatRange(min, max)` → ₱1.2M – ₱1.8M |
| `lib/utils/formatDate.ts` | `formatDate(s)` → "January 15, 2025" · `formatRelative(s)` → "2 days ago" |
| `lib/utils/generateSlug.ts` | `generateSlug(title)` → URL-safe lowercase hyphenated string |
| `lib/utils/calculateCost.ts` | `calculateCost(sqm, region, quality, data)` → `CostEstimate` with 12-phase breakdown. Multipliers: economy=1.0, standard=1.35, premium=1.75. Phase weights: site prep 3%, foundation 15%, structural 20%, roofing 10%, masonry 12%, electrical 8%, plumbing 8%, doors/windows 7%, tiling 6%, painting 5%, fixtures 4%, contingency 2%. |

### Authentication

| File | Purpose |
|------|---------|
| `components/providers/AuthProvider.tsx` | `'use client'` React context. Provides `user`, `session`, `loading`, `signOut()`. Listens to `supabase.auth.onAuthStateChange`. Exports `useAuth()` hook. |
| `app/auth/login/page.tsx` | Email + password login. RHF + Zod. Gold "Sign In" CTA. Forgot password + register links. Success → `/dashboard` or `?returnTo` param. |
| `app/auth/register/page.tsx` | Full name, email, password, confirm password, country/province/city, terms checkbox. On success → `/dashboard`. |
| `app/auth/forgot-password/page.tsx` | Email input → `supabase.auth.resetPasswordForEmail`. Success message state. |

### Layout chrome

| File | What it renders |
|------|----------------|
| `components/layout/Navbar.tsx` | Sticky. Starts transparent (over hero), transitions to `bg-navy/95 backdrop-blur shadow` after 20px scroll. Logo: Home icon (gold) + "PlanaCasa" (Playfair Display). Desktop nav: Browse Designs / Styles dropdown (9 categories with AnimatePresence) / Constructors / Cost Calculator / Guides. Auth: Sign In + Get Started (unauthenticated) or avatar dropdown with Dashboard / My Purchases / Wishlist / Sign Out. Active route highlighted. |
| `components/layout/MobileMenu.tsx` | shadcn Sheet from right. Logo top. Nav links. Styles as collapsible list. Auth section at bottom. Closes on navigation. |
| `components/layout/Footer.tsx` | 4-column grid (desktop) / stacked (mobile). Navy bg. Col 1: logo + tagline + description + 5 social icons (Facebook, Pinterest, YouTube, TikTok, Instagram as inline SVGs — lucide-react v1.17 dropped brand icons). Col 2: Quick Links. Col 3: Support. Col 4: Legal. Gold divider line. Copyright + architect disclaimer. |
| `components/layout/MobileBottomNav.tsx` | Fixed bottom, `md:hidden`. 4 items: Home / Gallery / Calculator / Dashboard. Gold dot + color on active route. 44px min touch target. White bg + shadow. |

### App shell files

| File | Purpose |
|------|---------|
| `app/loading.tsx` | Full-page spinner with gold PlanaCasa logo mark |
| `app/error.tsx` | `'use client'` error boundary. "Something went wrong" + retry button. |
| `app/not-found.tsx` | Branded 404 with navy/gold, link back to home. |
| `app/page.tsx` | Placeholder (overwritten in Phase 2). |

---

## Database — Supabase project: `lxsjhdelyjmirhzwjolp`

### Migration applied: `init_planacasa_schema`

**Applied via:** Supabase MCP `apply_migration` tool (not CLI — no personal access token available).  
**Source file committed at:** `supabase/migrations/0001_init.sql`

#### Tables created (11 total)

| Table | Primary key | Notable constraints |
|-------|-------------|---------------------|
| `users` | UUID → `auth.users` | `role user_role NOT NULL DEFAULT 'customer'` |
| `designs` | UUID | `slug UNIQUE NOT NULL` |
| `design_packages` | UUID | `package_name IN ('Basic','Standard','Premium')` |
| `purchases` | UUID | `transaction_id UNIQUE`, `payment_status IN (pending/completed/failed)` |
| `contractors` | UUID | — |
| `contractor_reviews` | UUID | `rating BETWEEN 1 AND 5`, `UNIQUE(contractor_id, user_id)` |
| `guides` | UUID | `slug UNIQUE NOT NULL` |
| `cost_calculator_data` | UUID | — |
| `customization_requests` | UUID | — |
| `newsletter_subscribers` | UUID | `email UNIQUE NOT NULL` |
| `wishlists` | UUID | `UNIQUE(user_id, design_id)` |

#### Enums created (5)

| Enum | Values |
|------|--------|
| `design_style` | modern_filipino, tropical_modern, european_modern, uk_contemporary, scandinavian, ofw_dream, small_lot, two_storey_rental, bungalow |
| `guide_category` | foundation, structural, roofing, electrical, plumbing, finishing, permits, budgeting, diy_tips |
| `customization_status` | pending, reviewing, quoted, completed |
| `listing_tier` | free, verified, featured |
| `user_role` | customer, admin |

#### Indexes created (8)

`idx_designs_slug`, `idx_designs_style`, `idx_designs_featured`, `idx_designs_bedrooms`, `idx_contractors_province`, `idx_contractors_featured`, `idx_purchases_user`, `idx_wishlists_user`

#### Triggers created (3)

| Trigger | Table | Function | Fires |
|---------|-------|----------|-------|
| `designs_updated_at` | `designs` | `update_updated_at()` | BEFORE UPDATE — sets `updated_at = NOW()` |
| `contractor_rating_update` | `contractor_reviews` | `update_contractor_rating()` | AFTER INSERT/UPDATE/DELETE — recomputes `average_rating` + `total_reviews` on parent contractor |
| `on_auth_user_created` | `auth.users` | `handle_new_user()` | AFTER INSERT — auto-creates `public.users` row on signup |

#### RLS policies (19 total, all tables)

| Table | Policies |
|-------|---------|
| `users` | Owner SELECT, Owner UPDATE, Admin SELECT all |
| `designs` | Public SELECT, Admin ALL |
| `design_packages` | Public SELECT, Admin ALL |
| `purchases` | Owner SELECT, Admin SELECT |
| `contractors` | Public SELECT, Admin ALL |
| `contractor_reviews` | Public SELECT, Auth INSERT (own), Owner UPDATE |
| `guides` | Public SELECT, Admin ALL |
| `cost_calculator_data` | Public SELECT |
| `customization_requests` | Owner ALL, Admin SELECT |
| `newsletter_subscribers` | Anon INSERT, Admin SELECT |
| `wishlists` | Owner ALL |

### Seed data applied (via Supabase MCP `execute_sql`)

**Source file committed at:** `supabase/seed.sql`

#### Designs (12 rows)

| Slug | Style | Beds | Baths | sqm | Cost range | Plan price | Featured |
|------|-------|------|-------|-----|-----------|-----------|---------|
| casa-rizal | modern_filipino | 4 | 3 | 180 | ₱2.7M–₱4.5M | ₱2,499 | ✓ |
| bahay-kubo-modern | modern_filipino | 3 | 2 | 120 | ₱1.8M–₱3.0M | ₱1,499 | — |
| batangas-modern | tropical_modern | 4 | 3 | 210 | ₱3.15M–₱5.25M | ₱2,999 | ✓ |
| davao-tropical | tropical_modern | 3 | 2 | 145 | ₱1.9M–₱3.2M | ₱1,799 | — |
| villa-europea | european_modern | 5 | 4 | 280 | ₱4.2M–₱7.0M | ₱3,499 | ✓ |
| metro-contemporary | uk_contemporary | 3 | 3 | 160 | ₱2.4M–₱4.0M | ₱2,499 | ✓ |
| nordic-narra | scandinavian | 3 | 2 | 130 | ₱1.95M–₱3.25M | ₱1,999 | — |
| ofw-pride-home | ofw_dream | 6 | 4 | 250 | ₱3.75M–₱6.25M | ₱3,499 | ✓ |
| metro-mini | small_lot | 3 | 2 | 90 | ₱1.35M–₱2.25M | ₱999 | ✓ |
| rental-plus | two_storey_rental | 4 | 4 | 200 | ₱3.0M–₱5.0M | ₱2,999 | — |
| baguio-bungalow | bungalow | 3 | 2 | 110 | ₱1.65M–₱2.75M | ₱1,499 | — |
| cebu-contemporary | bungalow | 3 | 2 | 125 | ₱1.875M–₱3.125M | ₱1,799 | ✓ |

#### Design packages (36 rows — 3 per design)

| Package | Price | Key inclusions |
|---------|-------|---------------|
| Basic | ₱999 | Floor plans (PDF), elevations, site plan, basic specs, 1 revision |
| Standard | ₱1,999 | + Structural/electrical/plumbing drawings, BoM, 3D renders (4 views), 3 revisions |
| Premium | ₱3,499 | + CAD files, detailed BoM with costs, mood board, as-built template, Gantt, permit-ready set, unlimited revisions 30 days, 1hr architect consult |

#### Contractors (6 rows)

| Business | Province | Region | Tier | Verified |
|----------|----------|--------|------|---------|
| BuildRight NCR | Metro Manila | NCR | featured | ✓ |
| Cebu Premier Builders | Cebu | Region VII | featured | ✓ |
| Davao Home Builders | Davao del Sur | Region XI | verified | ✓ |
| Pampanga Construction Group | Pampanga | Region III | verified | ✓ |
| Laguna Build & Design | Laguna | Region IV-A | free | — |
| Iloilo Builders Cooperative | Iloilo | Region VI | verified | ✓ |

#### Guides (5 rows)

| Slug | Category | Read time | Free |
|------|----------|-----------|------|
| foundation-types-philippine-soil | foundation | 8 min | ✓ |
| home-electrical-wiring-philippine-standards | electrical | 7 min | ✓ |
| plumbing-layout-philippine-homes | plumbing | 6 min | ✓ |
| roofing-guide-typhoon-resistance | roofing | 9 min | ✓ |
| building-permit-philippines-step-by-step | permits | 10 min | ✓ |

#### Cost calculator data (9 rows)

| Region | Province | Low/sqm | Mid/sqm | High/sqm |
|--------|----------|---------|---------|---------|
| NCR | Metro Manila | ₱18,000 | ₱25,000 | ₱38,000 |
| Region III | Pampanga | ₱14,000 | ₱19,000 | ₱28,000 |
| Region IV-A | Laguna | ₱14,500 | ₱20,000 | ₱30,000 |
| Region IV-A | Batangas | ₱13,500 | ₱18,500 | ₱27,000 |
| Region VII | Cebu | ₱15,000 | ₱21,000 | ₱32,000 |
| Region XI | Davao del Sur | ₱13,000 | ₱18,000 | ₱26,000 |
| Region VI | Iloilo | ₱12,500 | ₱17,000 | ₱25,000 |
| Region X | Misamis Oriental | ₱12,000 | ₱16,500 | ₱24,000 |
| Region VI | Negros Occidental | ₱12,000 | ₱16,000 | ₱23,000 |

---

## Commit 3 — `61c729f` — Phase 2: full homepage with 12 sections + animations

**Date:** 2026-05-29  
**Branch:** main  
**Files changed:** 17 files · +1,874 lines / −112 lines

### Shared components

| File | Lines | Purpose |
|------|-------|---------|
| `components/shared/ScrollReveal.tsx` | 49 | Framer Motion `useInView` wrapper. Props: `children`, `className?`, `delay?` (default 0), `direction?` (up/left/right). Triggers once at 15% viewport. Fades + slides from 30px offset. |
| `components/shared/AnimatedCounter.tsx` | 54 | rAF-based counter with ease-out-cubic easing. Triggers on `useInView`. Props: `target`, `duration?` (2000ms), `prefix?`, `suffix?`, `className?`. |
| `components/shared/LoadingSkeleton.tsx` | 57 | Three exports: `DesignCardSkeleton`, `ContractorCardSkeleton`, `GuideCardSkeleton` using shadcn `Skeleton`. |

### Homepage sections

| File | Lines | Data | Key features |
|------|-------|------|-------------|
| `components/home/HeroSection.tsx` | 254 | Static | Full-viewport. GSAP ScrollTrigger parallax (dynamic import in `useEffect` to avoid SSR). Staggered word-by-word headline animation. Glassmorphic search bar (3 dropdowns: style/budget/bedrooms). Bounce scroll arrow. Two pill CTAs. |
| `components/home/StatsSection.tsx` | 44 | Static | Navy bg. 4 `AnimatedCounter` instances (50+ designs, 9 styles, 80+ regions, 1000+ OFW families). Vertical dividers (hidden mobile). |
| `components/home/FeaturedDesigns.tsx` | 205 | **Supabase** | Client fetch: `designs` WHERE `featured=true` LIMIT 6. Loading → 6 × `DesignCardSkeleton`. Desktop 3-col grid, mobile horizontal scroll + snap. Inline `DesignCard`: 4:3 image, style badge (9 colour variants), bed/bath/sqm specs, build cost range, plan price, wishlist heart, "View Design →" CTA. |
| `components/home/StylesSection.tsx` | 135 | Static | Horizontal scrollable pill row (scrollbar hidden). 9 style pills with emoji icons → filtered gallery URLs. 3 tall featured style cards below (Modern Filipino, OFW Dream, European) with image overlays. |
| `components/home/HowItWorks.tsx` | 94 | Static | 4-step: Browse → Purchase → Match → Build. Horizontal dashed connector (desktop). Vertical stack (mobile). `ScrollReveal` stagger on each step. |
| `components/home/WhySection.tsx` | 72 | Static | 3 value prop cards: Climate-Adapted Designs / Complete Construction Details / Verified Local Contractors. Gold icon circles. Hover shadow. |
| `components/home/StyleShowcase.tsx` | 83 | Static | Navy bg. Cormorant Garamond italic subtitle. 2-column 500px-tall full-bleed images (European/UK left, Tropical right). Hover scale-105. |
| `components/home/CalculatorTeaser.tsx` | 144 | Computed | Inline calculator card. Floor area + region inputs. Hardcoded base rates (7 regions). `AnimatePresence` result animation. Links to `/calculator`. |
| `components/home/ContractorPreview.tsx` | 208 | **Supabase** | Client fetch: `contractors` WHERE `is_featured=true` LIMIT 3. Loading → 3 × `ContractorCardSkeleton`. Decorative region coverage strip (NCR/Luzon/Visayas/Mindanao). Contractor cards with verified/PCAB badges, specialization pills, star rating, price range. |
| `components/home/TestimonialsSection.tsx` | 89 | Hardcoded | Navy bg. 3 testimonials: Rodel & Marie Santos (Dubai OFW/Pampanga), Jennifer Magno (Cebu), Mark Dela Cruz (QC). Gold star ratings. Gold avatar initials. |
| `components/home/GuidesPreview.tsx` | 151 | **Supabase** | Client fetch: `guides` WHERE `is_free=true` LIMIT 3. Category-coloured top bands (amber/yellow/blue/slate/green). `line-clamp` excerpt. Read-time + "Read Guide →". |
| `components/home/NewsletterSection.tsx` | 129 | API write | Email input → POST `/api/newsletter`. `AnimatePresence` success/error states. Trust badges (No spam / Unsubscribe / Made for PH). |

### API route

| File | Method | Handler |
|------|--------|---------|
| `app/api/newsletter/route.ts` | POST | Zod validates `{ email, name?, source? }`. Admin client inserts into `newsletter_subscribers`. Returns 200 / 400 (invalid) / 409 (duplicate). |

### Homepage page

| File | Type | Notes |
|------|------|-------|
| `app/page.tsx` | Server component | Full OpenGraph + Twitter metadata. Renders all 12 sections in order. Replaced Phase 1 placeholder. |

### Fixes applied during Phase 2

| Issue | Fix |
|-------|-----|
| `ZodError.errors` → renamed in Zod v4 | Changed to `ZodError.issues` |
| Framer Motion `ease` array type mismatch | Typed as `[number, number, number, number]` tuple |
| `WhySection` needed event handlers | Added `'use client'` directive |

---

## Commit 4 — Phase 3: Gallery + Design Detail pages

**Date:** 2026-05-29
**Branch:** main
**Files changed:** 18 new files · shadcn components added (accordion, checkbox, radio-group, switch)

### shadcn components added
`accordion`, `checkbox`, `radio-group`, `switch` — needed for FilterSidebar (checkboxes/radio/toggle) and DesignTabs DIY accordion.

### Gallery components (`components/gallery/`)

| File | Lines | Purpose |
|------|-------|---------|
| `DesignCard.tsx` | — | Reusable design card. Style badge colour map (9 styles × unique colours). Wishlist heart toggle (auth-aware). Specs row (bed/bath/sqm). Build cost range + plan price. "View Design →" CTA linking to `/design/[slug]`. `priority` prop for LCP images. |
| `GalleryGrid.tsx` | — | Responsive grid: 1-col mobile / 2-col tablet / 3-col desktop. ScrollReveal stagger on first 6 cards. Appends DesignCardSkeleton × 3 while loading next page. Empty state with Reset Filters CTA. Invisible sentinel div for IntersectionObserver. |
| `FilterSidebar.tsx` | — | Desktop sticky panel (`w-72 sticky top-24`). 8 collapsible sections: Search (debounced 300ms), Style (9 checkboxes), Bedrooms (radio: Any/1/2/3/4/5+), Bathrooms (radio: Any/1/2/3+), Floors (radio: Any/1/2/3), Garage (switch), Build Cost (min/max number inputs), Sort By (dropdown). Active filter count badge. Reset All button. |
| `FilterMobile.tsx` | — | Fixed floating pill button `bottom-20 right-4` (above mobile bottom nav). Opens shadcn Sheet from bottom (85vh). Same filter sections as sidebar. "Apply Filters" + "Reset" buttons. |
| `GalleryClient.tsx` | — | `'use client'` orchestrator. Manages URL sync (`useRouter` + `useSearchParams`). IntersectionObserver infinite scroll (fetches next 12 via `/api/designs`). Auth-aware wishlist state. Renders FilterSidebar (desktop) + GalleryGrid + FilterMobile trigger. |

### API routes

| File | Methods | Logic |
|------|---------|-------|
| `app/api/designs/route.ts` | GET | URLSearchParams: `search` (ilike on title), `style` (comma-separated multi), `bedrooms`/`bathrooms`/`floors` (number filters), `garage` (bool), `cost_min`/`cost_max` (range), `sort` (featured/price_asc/price_desc/newest/area/bedrooms), `page` + `limit` (default 12). Returns `{ designs, total, page, hasMore }`. |
| `app/api/wishlist/route.ts` | GET / POST / DELETE | GET: returns all wishlisted design IDs for session user. POST: inserts `(user_id, design_id)` into `wishlists`. DELETE: removes row. All return 401 if unauthenticated. Uses server Supabase client (cookie-based session). |

### Gallery page

| File | Type | Notes |
|------|------|-------|
| `app/gallery/page.tsx` | Server component | SSR-first: reads `searchParams`, builds Supabase query, passes `{ initialDesigns, totalCount, initialFilters }` to `GalleryClient`. Navy header with "Browse House Plans" title + gold count badge. Metadata: "Browse House Plans \| PlanaCasa Philippines". |

### Design detail components (`components/design/`)

| File | Purpose |
|------|---------|
| `DesignHero.tsx` | 60vh full-width hero. GSAP ScrollTrigger parallax (0.4× speed, dynamic import in useEffect). Dark gradient overlay (bottom 60%). Breadcrumb + Playfair Display title + style badge overlaid bottom-left. |
| `ImageGallery.tsx` | Main 4:3 image display + horizontal thumbnail strip (80×60px, gold border on active). Lightbox: fixed full-screen overlay, prev/next arrows, X close, keyboard (←→ Escape), touch swipe (touchstart/touchend delta threshold 50px). |
| `DesignSpecs.tsx` | 2-column alternating-row specs table: floor area, lot area, bedrooms, bathrooms, floors, garage, style, build cost range, plan price. |
| `DesignTabs.tsx` | `'use client'` shadcn Tabs wrapper. 5 tabs: **Overview** (DesignSpecs + description + tags), **Floor Plans** (images grid or placeholder), **Cost Breakdown** (CostBreakdown component), **DIY Tips** (6-phase accordion: Site Prep / Foundation / Structural / Roofing / MEP / Finishing), **Climate Notes** (ClimateAdaptation component). |
| `CostBreakdown.tsx` | Calls `calculateCost()` with design's `floor_area_sqm`, NCR rates, standard quality. 12-phase table: min/avg/max per phase. Subtotal + contingency + total rows (navy/gold highlights). Horizontal proportional bar chart. Link to `/calculator`. Disclaimer note. |
| `ClimateAdaptation.tsx` | Style-aware static notes + design-specific `climate_notes` from DB. 4 climate category cards: Typhoon Resistance / Natural Ventilation / Flood Mitigation / Heat Management. |
| `PricingPackages.tsx` | 3 package cards stacked. Standard auto-selected with "RECOMMENDED" gold badge. Click to select. Full includes list with CheckCircle icons. "Buy Now" CTA (auth check → `/checkout?design=[slug]&package=[id]`). Payment/download trust row. Wishlist heart button. 5 social share buttons (Facebook, WhatsApp, Viber, Messenger, Pinterest). "Request Customization" link. Mobile: fixed bottom CTA bar (above bottom nav, with selected package + price). |
| `RelatedDesigns.tsx` | "You Might Also Like" heading. 3 DesignCard grid. |
| `ContractorMatch.tsx` | Navy section. Province select (20 major provinces). "Find Contractors →" routes to `/constructors?province=[selected]`. Disclaimer: "PlanaCasa connects you with independent contractors. We do not employ or guarantee any contractor's work." |

### Design detail page

| File | Type | Notes |
|------|------|-------|
| `app/design/[slug]/page.tsx` | Server component | Fetches design with `design_packages(*)` join. `notFound()` if slug missing. Fetches 3 related designs (same style). Dynamic metadata + OG image from `preview_images[0]`. Desktop: 65% left (DesignHero + ImageGallery + DesignTabs) / 35% right sticky (PricingPackages). Full-width below: RelatedDesigns + ContractorMatch. |

### Fixes applied during Phase 3

| Issue | Fix |
|-------|-----|
| Base-UI Accordion has different API from Radix | Removed `type="multiple"` prop — base-ui Accordion doesn't accept it |
| React 19 nullable refs | Changed `React.RefObject<HTMLDivElement>` → `React.RefObject<HTMLDivElement \| null>` throughout |

### Routes after Phase 3 build

| Route | Type | Description |
|-------|------|-------------|
| `/` | Static | Homepage (12 sections) |
| `/gallery` | Dynamic (SSR) | Gallery with server-side filter + client infinite scroll |
| `/design/[slug]` | Dynamic (SSR) | Individual design page |
| `/auth/login` | Static | Login page |
| `/auth/register` | Static | Register page |
| `/auth/forgot-password` | Static | Password reset page |
| `/api/designs` | Dynamic | GET designs with filters/pagination |
| `/api/newsletter` | Dynamic | POST newsletter subscription |
| `/api/wishlist` | Dynamic | GET/POST/DELETE wishlist items |
| `/_not-found` | Static | 404 page |

---

## Commit 5 — Phase 4: checkout, mock payment, downloads, dashboard, email service

**Date:** 2026-05-29
**Branch:** main
**Files:** 24 created · 1 modified · shadcn `textarea` added

### Infrastructure changes
- **Supabase Storage:** private bucket `plan-files` created (public=false) for gated plan downloads via signed URLs.

### Payment abstraction (`lib/payments/`)
| File | Purpose |
|------|---------|
| `types.ts` | `PaymentIntent`, `CreateIntentParams`, `PaymentProvider` interfaces |
| `mock.ts` | `MockPaymentProvider` — UUID intent IDs, `confirmIntent` returns 'completed' (or 'failed' if intentId contains 'fail' for testing) |
| `index.ts` | `getPaymentProvider(method)` → MockPaymentProvider for now (Stripe/PayMongo adapters slot in Phase 7) |

### Email service (`lib/email/`)
| File | Purpose |
|------|---------|
| `types.ts` | `EmailMessage`, `EmailService` interfaces |
| `templates.ts` | `welcomeEmail()`, `purchaseConfirmationEmail()` — inline-styled table HTML (navy/gold), uses NEXT_PUBLIC_SITE_URL |
| `console.ts` | `ConsoleEmailService` — logs boxed summary + text body to server console |
| `index.ts` | `getEmailService()` → ConsoleEmailService for now (ResendEmailService in Phase 7) |

### API routes
| File | Methods | Logic |
|------|---------|-------|
| `app/api/purchases/route.ts` | POST / GET | POST: auth-check → load package+design via admin → **derive amount server-side** (never trust client) → currency PHP (gcash/maya/card/bank) or USD (stripe_card) → mock createIntent+confirmIntent → insert purchase (method mapped to paymongo/stripe/mock) → upsert buyer profile → send confirmation email → 402 on failure. GET: list user purchases joined w/ design+package |
| `app/api/downloads/[purchaseId]/route.ts` | GET | Auth → ownership 403 → status!=completed 403 → count>=5 OR >30 days 410 → signed URL (300s TTL) → increment counter only on real file → demo JSON (no increment) if file missing |
| `app/api/customization/route.ts` | POST / GET | POST: auth → insert customization_request (status pending). GET: list user requests joined w/ design |
| `app/api/welcome/route.ts` | POST | Fire-and-forget welcome email on registration |

### Checkout & Success
| File | Purpose |
|------|---------|
| `app/checkout/page.tsx` | Server shell — reads `?design=slug&package=id`, fetches design+packages+profile, noindex metadata |
| `components/checkout/CheckoutClient.tsx` | Two-column (forms / sticky summary). Package radio cards, RHF+Zod buyer form, PayMongo/Stripe payment tabs (gcash/maya/card/bank/stripe_card), promo field (cosmetic), terms checkbox, demo-mode banner. POST /api/purchases → /success |
| `app/success/page.tsx` | Server — verifies purchase ownership, noindex |
| `components/checkout/SuccessClient.tsx` | Framer spring checkmark, order recap, "Download Your Plans" (handles demo/410/redirect), Find Contractor, FB share, dashboard links |

### Dashboard (`app/dashboard/`)
| File | Purpose |
|------|---------|
| `layout.tsx` | Sidebar nav (Overview/Purchases/Wishlist/Customization/Settings), user avatar+name, active highlight |
| `page.tsx` | Overview — 3 stat cards, recent purchases, empty-state nudge |
| `purchases/page.tsx` + `components/dashboard/PurchasesList.tsx` | Purchase cards: thumbnail, package badge, amount, status badge, DownloadButton (X/5), Find Contractor, Invoice dialog (window.print) |
| `components/dashboard/DownloadButton.tsx` | Calls /api/downloads, handles demo/410/redirect, updates used count |
| `wishlist/page.tsx` + `components/dashboard/WishlistGrid.tsx` | DesignCard grid, optimistic remove via DELETE /api/wishlist |
| `customization/page.tsx` + `components/dashboard/CustomizationList.tsx` | New Request dialog, 4-step status stepper (Pending→Reviewing→Quoted→Completed) |
| `settings/page.tsx` + `components/dashboard/SettingsForm.tsx` | Edit profile (RLS owner update), password reset email, delete-account stub |

### Shared helpers
- `lib/constants/locations.ts` — 20 major provinces + countries list (deduped)
- `lib/utils/triggerDownload.ts` — shared download handler (Success + DownloadButton)

### Modified
- `app/auth/register/page.tsx` — fire-and-forget POST /api/welcome after sign-up

### Deviations
1. `Facebook` icon missing in lucide-react v1.17 → used `Share2` for FB share button
2. Base-UI Dialog has no `asChild` → styled DialogTrigger/Close directly
3. CheckoutClient takes `userEmail` + `profile` separately (auth email not reliably on public.users row)

### Known limitation (flagged for later)
- Download counter increment is read-then-write (non-atomic). Concurrent requests could over-count by 1-2. Negligible for owned digital goods; harden with a Postgres atomic increment RPC later if needed.

---

## What is NOT built yet (deferred to later phases)

| Feature | Target phase |
|---------|-------------|
| Contractor marketplace page | Phase 5 |
| Cost calculator full page | Phase 5 |
| DIY Guides listing + detail | Phase 5 |
| Admin panel | Phase 6 |
| Legal pages | Phase 6 |
| PWA manifest | Phase 6 |
| SEO sitemap + robots.txt | Phase 6 |
| PayMongo integration | Phase 7 |
| Stripe integration | Phase 7 |
| Resend email integration | Phase 7 |

---

## Environment variables reference

| Variable | Value source | Used in |
|----------|-------------|---------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase project settings | Client + Server + Admin |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase project settings | Client + Server |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase project settings | Admin client (route handlers only) |
| `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | Stripe dashboard | Checkout (Phase 7) |
| `STRIPE_SECRET_KEY` | Stripe dashboard | Route handlers (Phase 7) |
| `STRIPE_WEBHOOK_SECRET` | Stripe webhook settings | Webhook handler (Phase 7) |
| `PAYMONGO_PUBLIC_KEY` | PayMongo dashboard | Checkout (Phase 7) |
| `PAYMONGO_SECRET_KEY` | PayMongo dashboard | Route handlers (Phase 7) |
| `PAYMONGO_WEBHOOK_SECRET` | PayMongo webhook settings | Webhook handler (Phase 7) |
| `RESEND_API_KEY` | Resend dashboard | Email service (Phase 7) |
| `NEXT_PUBLIC_GA_MEASUREMENT_ID` | Google Analytics | Analytics (Phase 6) |
| `NEXT_PUBLIC_SITE_URL` | Deployment URL | OG tags, email links |
| `ADMIN_EMAIL` | Manual config | Contact + admin notifications |

---

## GitHub repository

**URL:** https://github.com/devaidusty/PlanaCasa  
**Branch strategy:** `main` only (single developer). Feature branches when team grows.  
**PAT:** Provided by user, time-limited. Revoke after confirming push works.

---

*Last updated: Phase 3 complete — 2026-05-29*
